import SwiftUI

extension Color {
    static let terminalBackground = Color.black
    static let terminalForeground = Color.white
    static let terminalGreen = Color(red: 0.2, green: 0.8, blue: 0.4)
}
